package day26exceptions;

public class Err {

	
	/*
	  1) When we get Exception, we can handle them by throwing exceptions or using try- catch block.
	  	But when we get "Error " we can do nothing. Exceptions can be handled but Error not
	  
	  2) Common ereors are a) "Out of Memory error " : it occurs when the Heap memory is full for any reason. 
	  					   b) "StackoverflowError" : it occurs when the Stack memory is full for any reason,
	 */
	
	/*
	 we learned a) final ==> final variable : Value cannot be changed, must be initilialized , use uppercases
						 final method : Cannot be overridden
						 final class: cannot be extended
			    b) finally ==> it is used in try-catch block and it runs evertime
			    c) finalize==> Garbage collector scans the memory constantly. if There is something
			    to remove Java "finalizes " is first then remove it from the memory. 
			    Finalize is a method, you can call the finalized ( ) if you need but jAVA 
			    runs it whenever it wants
			
	 */
}
